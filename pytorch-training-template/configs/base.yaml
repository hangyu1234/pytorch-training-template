seed: 42


data:
  num_samples: 1000
  num_features: 3
  val_ratio: 0.2


model:
  input_dim: 3
  output_dim: 1


training:
  epochs: 100
  batch_size: 32
  lr: 0.01
  device: auto


checkpoint:
  dir: checkpoints


logging:
  dir: logs

output:
  dir: outputs